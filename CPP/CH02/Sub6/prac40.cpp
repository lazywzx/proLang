#include <iostream>
#include <string>

using std::cout;
using std::endl;
using std::string;

// 自定义Sales_data类

struct Sales_data {
    string isbn = "";
    unsigned int count = 0;
    unsigned int sale = 0;
};

int main() {
    return 0;
}
